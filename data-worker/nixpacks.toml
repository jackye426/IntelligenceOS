[phases.setup]
nixPkgs = ["python311", "ffmpeg", "git"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip install \"marketing-pipeline[media] @ git+https://github.com/jackye426/IntelligenceOS.git@main#subdirectory=marketing-pipeline\"",
]

[start]
cmd = "python main.py"
