[phases.setup]
nixPkgs = ["python311", "ffmpeg", "git"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip install \"marketing-pipeline @ git+https://github.com/jackye426/IntelligenceOS.git@main#subdirectory=marketing-pipeline\"",
  "pip install yt-dlp",
]

[start]
cmd = "python main.py"
