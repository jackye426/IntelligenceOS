# Railway Railpack (default). Root Directory = data-worker.
[build]
builder = "RAILPACK"

[deploy]
startCommand = "python -m playwright install chromium && python main.py"
restartPolicyType = "ON_FAILURE"
