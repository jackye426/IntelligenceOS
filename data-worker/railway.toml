# Railway: data-worker (Root Directory = data-worker, builder = Nixpacks)
[build]
builder = "NIXPACKS"
nixpacksConfigPath = "nixpacks.toml"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
