"""Person resolve lane — stub for later email / LinkedIn enrichment."""

from __future__ import annotations


def resolve_person(name: str, clinic_hint: str = "") -> dict:
    return {"status": "stub", "name": name, "clinic_hint": clinic_hint}
